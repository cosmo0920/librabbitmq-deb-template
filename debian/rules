#!/usr/bin/make -f
# -*- makefile -*-

# Uncomment this to turn on verbose mode.
#export DH_VERBOSE=1

%:
	dh $@  --buildsystem=makefile

build: build-stamp
build-stamp:
	dh_autoreconf
	dh_auto_configure -- --enable-static
	dh build
	touch build-stamp
